<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@mipmap/ic_red_heart" />
    <!-- Selected state -->
    <item android:state_checked="true" android:drawable="@mipmap/ic_red_heart" />
    <!-- Default state -->
    <item android:drawable="@mipmap/ic_heart" />
</selector>
